{
  "name": "@solo-guardian/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for Solo Guardian",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:auth": "playwright test auth.spec.ts",
    "test:checkin": "playwright test check-in.spec.ts",
    "codegen": "playwright codegen http://localhost:5173"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.0.0"
  }
}
