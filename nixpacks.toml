[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @solo-guardian/types build",
  "cd apps/backend && pnpm run prisma:generate && pnpm run build"
]

[start]
cmd = "cd apps/backend && node dist/main"
